<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tenxgames.aisd">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Lab2Activity" />
        <activity android:name=".Lab1Activity" />
        <activity android:name=".Lab3Activity" />
        <activity android:name=".Lab4Activity" />
        <activity android:name=".SwapsTableActivity" />
        <activity android:name=".Lab5Activity" />
        <activity android:name=".SortsLogActivity" />
        <activity android:name=".Lab6Activity" />
        <activity android:name=".Lab7Activity"></activity>
    </application>

</manifest>